{
  "name": "tex",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "codemirror": "^6.0.1",
    "esbuild": "^0.18.6"
  },
  "scripts": {
    "build": "esbuild client/index.js --bundle --outfile=nginx/build/index.js",
    "watch": "npm run build -- --watch"
  }
}
